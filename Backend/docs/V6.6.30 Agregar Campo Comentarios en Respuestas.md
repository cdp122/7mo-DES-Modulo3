# V6.6.30 Agregar Campo Comentarios en Respuestas y Validaciones
## Objetivos

- Agregar un campo de comentarios. Porque en la colección de respuestas he olvidado agregar un campo de feedback, donde se emiten comentarios sobre compromisos personales, y opiniones acerca del programa. Este ultimo es nuevo, sin embargo ahora la colección se actualiza:

Antes: 
```json
{
    "_id": ObjectId("..."),
    "datos_docente": {
        "cedula": "1234567890",
        "nombre": "María López",
    },
    "respuestas": [
        {
            "reactivo_codigo": "1.1", // Referencia al reactivo respondido
            "valor": 3 // Valor seleccionado en la escala Likert (0-4)
        },
        ...
    ],
    "resultados": {
        "subtotales": { "D1": 16, "D2": 12, "D3": 9 },
        "indices_dimensionales": { "ID1": 80.0, "ID2": 60.0, "ID3": 45.0 },
        "IGPP": 61.7,
        "dimension_prioritaria": "D3"
    },
    "version": "V6.6.22"
}
```

Ahora: 
```json
{
    "_id": ObjectId("..."),
    "cedula_docente": "1234567890",
    "respuestas": [
        {
            "reactivo_codigo": "1.1", // Referencia al reactivo respondido
            "valor": 3 // Valor seleccionado en la escala Likert (0-4)
        },
        ...
    ],
    "comentarios": {
        "compromiso_personal": "Me comprometo a mejorar mi participación en clase.",
        "opiniones_programa": "El programa es muy útil, pero podría incluir más ejemplos"
    },
    "resultados": {
        "subtotales": { "D1": 16, "D2": 12, "D3": 9 },
        "indices_dimensionales": { "ID1": 80.0, "ID2": 60.0, "ID3": 45.0 },
        "IGPP": 61.7,
        "dimension_prioritaria": "D3"
    },
    "version": "V6.6.30"
}
```

- Modificar los tests para validar ahora el nuevo flujo. El flujo actual es subir las respuestas, después de desplegar los resultados se envían los comentarios. Si alguno esta vacío, debe guardarse el campo y guardar null. Si los 2 están vacíos, se guarda null en ambos campos. Si ambos tienen contenido, se guardan ambos.
- Crear los tests con validación de campos. De paso ya que voy a modificar este esquema me encargaré de crear las validaciones de los campos. Tales como validar cédula ecuatoriana, que las respuestas sean solo de 0 a 4, y que los comentarios no excedan los 500 caracteres. En la colección de administradores también hay cédula, asi que por igual en el CRUD de admin voy a validar la cédula ecuatoriana.
- Modificar el esquema de Administradores. Ya no se necesita el campo de email, por esta razón este campo se elimina.

Antes:
```json
    {
        "_id": ObjectId("..."),
        "cedula": "1234567890",
        "nombre": "Juan Pérez",
        "email": "juan.perez@example.com",
        "password": "hashed_password",
        "version": "V6.6.22"
    }
```

Ahora:
```json
    {
        "_id": ObjectId("..."),
        "cedula": "1234567890",
        "nombre": "Juan Pérez",
        "password": "hashed_password",
        "version": "V6.6.30"
    }
```

- Validar que el CRUD de Administradores verifique la exigencia de un JWT válido. Este no puede usarse ni si quiera la lectura sin un JWT válido. Esto es para garantizar que solo los administradores puedan acceder a la información de los docentes y sus evaluaciones.

## Walktrough
### 2026-06-30
- **10:15:** Se han agregado las pruebas de verificación de las 15 respuestas válidas, validación de cédula y las respectivas validaciones de los comentarios. Se ha agregado el campo de comentarios en la colección de respuestas y se han modificado los tests para validar el nuevo flujo. Se ha creado un test para verificar que los comentarios no excedan los 500 caracteres. Se ha modificado el CRUD de administradores para validar la cédula ecuatoriana. Faltaría crear las validaciones de cédula en el CRUD de Admin y sus respectivas pruebas.

- **Explicación Técnica:**
- **Archivos Modificados (9):**
| Archivo | Cambios realizados |
|---------|-------------------|
| `src/domain/models/evaluacion.model.ts` | Eliminado `DatosDocente`, agregado `Comentarios` interface (`compromiso_personal: string \| null`, `opiniones_programa: string \| null`), actualizado `Evaluacion` con `cedula_docente` y `comentarios`, eliminado `docente_nombre` de `ResultadosInterpretados`, agregado `ComentarEvaluacionDTO` |
| `src/domain/repositories/evaluacion.repository.port.ts` | Agregado método `agregarComentarios(id: string, comentarios: Comentarios): Promise<Evaluacion \| null>` |
| `src/domain/services/calculos.service.ts` | `interpretarResultados` usa `evaluacion.cedula_docente` en vez de `evaluacion.datos_docente.cedula`, eliminado `docente_nombre` |
| `src/infrastructure/adapters/database/mongodb/models/evaluacion.schema.ts` | Eliminado `DatosDocenteSchema`, agregado `ComentariosSchema` (con `default: null`), `cedula_docente` como string requerido, `comentarios` con `default: null`, versión por defecto `V6.6.30` |
| `src/infrastructure/adapters/database/mongodb/repositories/evaluacion.repository.ts` | Nuevo método `agregarComentarios`, `mapearADominio` mapea `cedula_docente` y `comentarios`, query `obtenerPorDocenteCedula` usa `cedula_docente` |
| `src/infrastructure/adapters/graphql/schemas/evaluacion.graphql` | Declarado scalar `CedulaEcuatoriana`, tipo `Comentarios` (campos nullable), input `ComentariosInput`, mutation `agregarComentarios(evaluacionId: ID!, input: ComentariosInput!): Evaluacion!`, eliminados tipos `DatosDocente` y `DatosDocenteInput` |
| `src/infrastructure/adapters/graphql/schemas/administrador.graphql` | Scalar `CedulaEcuatoriana` aplicado a campos `cedula` en type `Administrador`, input `CrearAdministradorInput` y query `buscarAdminPorCedula` |
| `src/infrastructure/adapters/graphql/resolvers/evaluacion.resolver.ts` | Registrado scalar `CedulaEcuatoriana`, validación de max 500 caracteres en comentarios, mutation `agregarComentarios` con normalización de strings vacíos a `null` |
| `src/infrastructure/adapters/graphql/server/server.ts` | Scalar `CedulaEcuatoriana` registrado en `makeExecutableSchema` |

- **Archivo creado (1):**

| Archivo | Contenido |
|---------|-----------|
| `src/infrastructure/adapters/graphql/scalars/cedulaEcuatoriana.ts` | Scalar GraphQL `CedulaEcuatoriana` con algoritmo de validación: 10 dígitos, provincia 01-24, dígito verificador con algoritmo de módulo 10. Exporta también función utilitaria `esCedulaEcuatorianaValida()` |

- **Flujo implementado:**
1. **Crear evaluación** → Se envía `cedula_docente` + 15 respuestas. Los campos `comentarios` se inicializan en `null`.
2. **Ver resultados** → Query `obtenerResultadosEvaluacion(id)` retorna índices interpretados.
3. **Enviar comentarios** → Mutation `agregarComentarios(evaluacionId, input)` actualiza los campos de comentario. Si un campo viene vacío, se guarda `null`.

- **Validaciones implementadas:**
- **Cédula ecuatoriana**: 10 dígitos, province 01-24, dígito verificador correcto (scalar GraphQL)
- **Respuestas**: valores entre 0 y 4 (validación en resolver)
- **Comentarios**: máximo 500 caracteres por campo (validación en resolver)

- **Tests (12/12 passing):**

| CP | Descripción | Estado |
|----|-------------|--------|
| CP-01 | Crear evaluación con 15 respuestas válidas (verifica `comentarios` retorna `{null, null}`) | ✓ |
| CP-02 | Rechazar evaluación con < 15 respuestas | ✓ |
| CP-03 | Rechazar cédula con dígito verificador incorrecto | ✓ |
| CP-04 | Rechazar cédula con letras | ✓ |
| CP-05 | Rechazar cédula con provincia inválida (>= 25) | ✓ |
| CP-06 | Rechazar cédula con longitud incorrecta | ✓ |
| CP-07 | Rechazar valores de respuestas fuera de rango (0-4) | ✓ |
| CP-08 | Guardar ambos comentarios con contenido | ✓ |
| CP-09 | Guardar `null` en ambos campos cuando vacíos | ✓ |
| CP-10 | Guardar `null` en un campo cuando solo uno vacío | ✓ |
| CP-11 | Rechazar comentarios que excedan 500 caracteres | ✓ |
| CP-12 | Rechazar evaluación no encontrada en `agregarComentarios` | ✓ |

- **10:56:** Se han completado las validaciones de cédula en el CRUD de administradores y se han agregado los tests correspondientes. Ahora el CRUD de administradores requiere un JWT válido para todas las operaciones, incluyendo lectura. Se han actualizado los tests para reflejar este cambio. Se ha eliminado el uso del campo de email en la colección de administradores y se han modificado los tests para reflejar este cambio. Se ha actualizado la versión a V6.6.30.
- **11:25:** Por alguna razón se ha quedado el campo obsoleto "rol", este campo no debería haber existido desde la versión 6.6.22. Asi que se ha eliminado del sistema completamente. 